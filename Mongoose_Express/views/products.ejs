<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Products</title>
</head>
<body>
    <h1>All Products:</h1>
    <ul>
    <% products.forEach(element => { %>
        <li id="<%= element._id %> ">
            Name: <%= element.name %>
            <a href="/products/<%= element._id %>">Details...</a>
        </li>        
    <% }) %>
    </ul>

    <a href="/products/new">Add new Product</a>
    </body>
</html>